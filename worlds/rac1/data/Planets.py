from typing import NamedTuple, Sequence

from .Locations import *
from worlds.rac1.constants.locations.planets import RAC1PLANET
from ..constants.pools import RAC1POOL


class PlanetData(NamedTuple):
    name: str
    number: int
    locations: Sequence[LocationData] = []

GENERAL = PlanetData(RAC1PLANET.GENERAL, 0, [
    HOVERBOARD_TRICKY
])

NOVALIS = PlanetData(RAC1PLANET.NOVALIS, 1, [
    NOVALIS_PLUMBER,
    NOVALIS_MAYOR,
    NOVALIS_VENDOR_PYROCITOR,
    NOVALIS_SEWER_GOLD_BOLT,
    NOVALIS_CAVES_GOLD_BOLT,
    NOVALIS_UNDERWATER_CAVES_GOLD_BOLT,
    NOVALIS_GOLD_WEAPON_1,
    NOVALIS_GOLD_WEAPON_2,
    NOVALIS_GOLD_WEAPON_3,
    NOVALIS_GOLD_WEAPON_4,
    NOVALIS_GOLD_WEAPON_5,
    NOVALIS_GOLD_WEAPON_6,
    NOVALIS_GOLD_WEAPON_7,
    NOVALIS_GOLD_WEAPON_8,
    NOVALIS_GOLD_WEAPON_9,
    NOVALIS_GOLD_WEAPON_10,
    NOVALIS_SKILLPOINT,
])

ARIDIA = PlanetData(RAC1PLANET.ARIDIA, 2, [
    ARIDIA_HOVERBOARD,
    ARIDIA_TRESPASSER,
    ARIDIA_SONIC_SUMMONER,
    ARIDIA_TRESPASSER_GOLD_BOLT,
    ARIDIA_ISLAND_GOLD_BOLT,
    ARIDIA_MAGNEBOOTS_GOLD_BOLT,
    ARIDIA_SANDSHARK_GOLD_BOLT,
    ARIDIA_SWING_IT,
    ARIDIA_TRANSPORTED,
])

KERWAN = PlanetData(RAC1PLANET.KERWAN, 3, [
    KERWAN_SWINGSHOT,
    KERWAN_HELIPACK,
    KERWAN_TRAIN_INFOBOT,
    KERWAN_VENDOR_BLASTER,
    KERWAN_BELOW_SHIP_GOLD_BOLT,
    KERWAN_TRAIN_STATION_GOLD_BOLT,
    KERWAN_LONE_TOWER_GOLD_BOLT,
    KERWAN_STRIKE_A_POSE,
    KERWAN_BLIMPY,
    KERWAN_QWARKTASTIC,
])

EUDORA = PlanetData(RAC1PLANET.EUDORA, 4, [
    EUDORA_HENCHMAN,
    EUDORA_SUCK_CANNON,
    EUDORA_VENDOR_GLOVE_OF_DOOM,
    EUDORA_GOLD_BOLT,
    EUDORA_ANY_TEN,
])

RILGAR = PlanetData(RAC1PLANET.RILGAR, 5, [
    RILGAR_QUARK_INFOBOT,
    RILGAR_PLATINUM_ZOOMERATOR,
    RILGAR_MINE_GLOVE,
    RILGAR_RYNO,
    RILGAR_MAZE_GOLD_BOLT,
    RILGAR_WATERWORKS_GOLD_BOLT,
    RILGAR_CLUCK_CLUCK,
    RILGAR_SPEEDY,
])

BLARG = PlanetData(RAC1PLANET.BLARG, 6, [
    BLARG_HYDRODISPLACER,
    BLARG_EXPLOSION_INFOBOT,
    BLARG_GRINDBOOTS,
    BLARG_VENDOR_TAUNTER,
    BLARG_OUTSIDE_GOLD_BOLT,
    BLARG_SWARMER_GOLD_BOLT,
    BLARG_GIRL_TROUBLE,
])

UMBRIS = PlanetData(RAC1PLANET.UMBRIS, 7, [
    UMBRIS_SNAGGLEBEAST_INFOBOT,
    UMBRIS_PRESSURE_PUZZLE_GOLD_BOLT,
    UMBRIS_JUMP_DOWN_GOLD_BOLT,
])

BATALIA = PlanetData(RAC1PLANET.BATALIA, 8, [
    BATALIA_VENDOR_DEVASTATOR,
    BATALIA_GRINDRAIL_INFOBOT,
    BATALIA_COMMANDER_INFOBOT,
    BATALIA_METAL_DETECTOR,
    BATALIA_CLIFFSIDE_GOLD_BOLT,
    BATALIA_TRESPASSER_GOLD_BOLT,
    BATALIA_JUMPER,
    BATALIA_ACCURACY_COUNTS,
    BATALIA_EAT_LEAD,
])

GASPAR = PlanetData(RAC1PLANET.GASPAR, 9, [
    GASPAR_VENDOR_WALLOPER,
    GASPAR_PILOT_HELMET,
    GASPAR_SWINGSHOT_GOLD_BOLT,
    GASPAR_VOLCANO_GOLD_BOLT,
    GASPAR_DESTROYED,
    GASPAR_GUNNER,
])

ORXON = PlanetData(RAC1PLANET.ORXON, 10, [
    ORXON_VENDOR_VISIBOMB,
    ORXON_CLANK_INFOBOT,
    ORXON_RATCHET_INFOBOT,
    ORXON_CLANK_MAGNEBOOTS,
    ORXON_PREMIUM_NANOTECH,
    ORXON_ULTRA_NANOTECH,
    ORXON_CLANK_GOLD_BOLT,
    ORXON_VISIBOMB_GOLD_BOLT,
])

POKITARU = PlanetData(RAC1PLANET.POKITARU, 11, [
    POKITARU_VENDOR_DECOY_GLOVE,
    POKITARU_O2_MASK,
    POKITARU_SEWER_PERSUADER,
    POKITARU_THRUSTER_PACK,
    POKITARU_GOLD_BOLT,
])

HOVEN = PlanetData(RAC1PLANET.HOVEN, 12, [
    HOVEN_VENDOR_DRONE_DEVICE,
    HOVEN_TURRET_INFOBOT,
    HOVEN_HYDRO_PACK,
    HOVEN_RARITANIUM,
    HOVEN_WATER_GOLD_BOLT,
    HOVEN_WALLJUMP_GOLD_BOLT,
])

GEMLIK = PlanetData(RAC1PLANET.GEMLIK, 13, [
    GEMLIK_QUARK_FIGHT,
    GEMLIK_GOLD_BOLT,
])

OLTANIS = PlanetData(RAC1PLANET.OLTANIS, 14, [
    OLTANIS_VENDOR_TESLA_CLAW,
    OLTANIS_INFOBOT,
    OLTANIS_PDA,
    OLTANIS_MORPH_O_RAY,
    OLTANIS_MAIN_GOLD_BOLT,
    OLTANIS_MAGNET_GOLD_BOLT_1,
    OLTANIS_MAGNET_GOLD_BOLT_2,
    OLTANIS_FINAL_GOLD_BOLT,
])

QUARTU = PlanetData(RAC1PLANET.QUARTU, 15, [
    QUARTU_GIANT_CLANK_INFOBOT,
    QUARTU_BOLT_GRABBER,
    QUARTU_INFILTRATE_INFOBOT,
    QUARTU_MOM_GOLD_BOLT,
    QUARTU_CODEBOT_GOLD_BOLT,
])

KALEBO = PlanetData(RAC1PLANET.KALEBO_III, 16, [
    KALEBO_HOLOGUISE,
    KALEBO_MAP_O_MATIC,
    KALEBO_GRIND_GOLD_BOLT,
    KALEBO_BREAK_ROOM_GOLD_BOLT,
])

FLEET = PlanetData(RAC1PLANET.FLEET, 17, [
    FLEET_INFOBOT,
    FLEET_CODEBOT,
    FLEET_WATER_GOLD_BOLT,
    FLEET_ROBOT_GOLD_BOLT,
])

VELDIN = PlanetData(RAC1PLANET.VELDIN, 18, [
    VELDIN_TAUNTER_GOLD_BOLT,
    VELDIN_HALFWAY_GOLD_BOLT,
    VELDIN_GRIND_GOLD_BOLT,
    VELDIN_DREK,
])

LOGIC_PLANETS: Sequence[PlanetData] = [
    GENERAL,
    NOVALIS,
    ARIDIA,
    KERWAN,
    EUDORA,
    RILGAR,
    BLARG,
    UMBRIS,
    BATALIA,
    GASPAR,
    ORXON,
    POKITARU,
    HOVEN,
    GEMLIK,
    OLTANIS,
    QUARTU,
    KALEBO,
    FLEET,
    VELDIN,
]

ALL_LOCATIONS: Sequence[LocationData] = [
    location
    for locations in [planet.locations for planet in LOGIC_PLANETS]
    for location in locations
]

location_table_by_name: dict[str, LocationData] = {location.name: location for location in ALL_LOCATIONS}
location_groups: dict[str, set[str]] = {
    RAC1PLANET.NOVALIS: set(loc.name for loc in ALL_LOCATIONS if loc.planet in NOVALIS),
    RAC1PLANET.ARIDIA: set(loc.name for loc in ALL_LOCATIONS if loc.planet in ARIDIA),
    RAC1PLANET.KERWAN: set(loc.name for loc in ALL_LOCATIONS if loc.planet in KERWAN),
    RAC1PLANET.EUDORA: set(loc.name for loc in ALL_LOCATIONS if loc.planet in EUDORA),
    RAC1PLANET.RILGAR: set(loc.name for loc in ALL_LOCATIONS if loc.planet in RILGAR),
    RAC1PLANET.BLARG: set(loc.name for loc in ALL_LOCATIONS if loc.planet in BLARG),
    RAC1PLANET.UMBRIS: set(loc.name for loc in ALL_LOCATIONS if loc.planet in UMBRIS),
    RAC1PLANET.BATALIA: set(loc.name for loc in ALL_LOCATIONS if loc.planet in BATALIA),
    RAC1PLANET.GASPAR: set(loc.name for loc in ALL_LOCATIONS if loc.planet in GASPAR),
    RAC1PLANET.ORXON: set(loc.name for loc in ALL_LOCATIONS if loc.planet in ORXON),
    RAC1PLANET.POKITARU: set(loc.name for loc in ALL_LOCATIONS if loc.planet in POKITARU),
    RAC1PLANET.HOVEN: set(loc.name for loc in ALL_LOCATIONS if loc.planet in HOVEN),
    RAC1PLANET.GEMLIK: set(loc.name for loc in ALL_LOCATIONS if loc.planet in GEMLIK),
    RAC1PLANET.OLTANIS: set(loc.name for loc in ALL_LOCATIONS if loc.planet in OLTANIS),
    RAC1PLANET.QUARTU: set(loc.name for loc in ALL_LOCATIONS if loc.planet in QUARTU),
    RAC1PLANET.KALEBO_III: set(loc.name for loc in ALL_LOCATIONS if loc.planet in KALEBO),
    RAC1PLANET.FLEET: set(loc.name for loc in ALL_LOCATIONS if loc.planet in FLEET),
    RAC1PLANET.VELDIN: set(loc.name for loc in ALL_LOCATIONS if loc.planet in VELDIN),
    RAC1POOL.WEAPONS: set(loc.name for loc in ALL_LOCATIONS if loc.pools.issubset(RAC1POOL.WEAPONS) and len(loc.pools)),
    RAC1POOL.GOLD_WEAPONS: set(
        loc.name for loc in ALL_LOCATIONS if loc.pools.issubset(RAC1POOL.GOLD_WEAPONS) and len(loc.pools)),
    RAC1POOL.GADGETS: set(loc.name for loc in ALL_LOCATIONS if loc.pools.issubset(RAC1POOL.GADGETS) and len(loc.pools)),
    RAC1POOL.PACKS: set(loc.name for loc in ALL_LOCATIONS if loc.pools.issubset(RAC1POOL.PACKS) and len(loc.pools)),
    RAC1POOL.HELMETS: set(loc.name for loc in ALL_LOCATIONS if loc.pools.issubset(RAC1POOL.HELMETS) and len(loc.pools)),
    RAC1POOL.BOOTS: set(loc.name for loc in ALL_LOCATIONS if loc.pools.issubset(RAC1POOL.BOOTS) and len(loc.pools)),
    RAC1POOL.EXTRA_ITEMS: set(loc.name for loc in ALL_LOCATIONS if loc.pools.issubset(RAC1POOL.EXTRA_ITEMS) and len(loc.pools)),
    RAC1POOL.GOLD_BOLTS: set(
        loc.name for loc in ALL_LOCATIONS if loc.pools.issubset(RAC1POOL.GOLD_BOLTS) and len(loc.pools)),
    RAC1POOL.INFOBOTS: set(loc.name for loc in ALL_LOCATIONS if loc.pools.issubset(RAC1POOL.INFOBOTS) and len(loc.pools)),
    RAC1POOL.SKILLPOINT: set(
        loc.name for loc in ALL_LOCATIONS if loc.pools.issubset(RAC1POOL.SKILLPOINT) and len(loc.pools)),
}
